
export const userSignup=(req,res)=>{
    res.status(200).json({message:"helo there from the userSignup"});
}
export const userLogin=(req,res)=>{
    res.status(200).json({message:"helo there from the userLogin"});
}